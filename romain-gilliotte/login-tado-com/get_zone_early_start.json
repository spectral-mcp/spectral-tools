{
  "name": "get_zone_early_start",
  "description": "Retrieve the early start settings for a specific zone, including whether early start (preheating) is enabled for automatic zone heating before the scheduled time block begins",
  "parameters": {
    "properties": {
      "home_id": {
        "description": "The unique identifier of the home/account",
        "type": "integer"
      },
      "zone_id": {
        "description": "The unique identifier of the zone (room)",
        "type": "integer"
      }
    },
    "required": [
      "home_id",
      "zone_id"
    ],
    "type": "object"
  },
  "request": {
    "method": "GET",
    "url": "https://my.tado.com/api/v2/homes/{home_id}/zones/{zone_id}/earlyStart",
    "headers": {},
    "query": {},
    "body": null,
    "content_type": "application/json"
  },
  "requires_auth": true
}