{
  "name": "get_active_orders",
  "description": "Retrieves the user's active parking orders with pagination support. Returns a list of active parking reservations/bookings that the user has placed, filtered by status and limited by count parameter.",
  "parameters": {
    "properties": {
      "count": {
        "default": 1,
        "description": "Maximum number of orders to return per page",
        "type": "integer"
      },
      "includeHeaders": {
        "default": true,
        "description": "Whether to include header information in the response",
        "type": "boolean"
      },
      "tab": {
        "default": "active",
        "description": "Filter for order status, typically 'active' for current/ongoing orders",
        "type": "string"
      }
    },
    "required": [
      "tab"
    ],
    "type": "object"
  },
  "request": {
    "method": "GET",
    "url": "https://my.flowbirdapp.com/api/search/order/",
    "headers": {},
    "query": {
      "count": {
        "$param": "count"
      },
      "includeHeaders": {
        "$param": "includeHeaders"
      },
      "tab": {
        "$param": "tab"
      }
    },
    "body": null,
    "content_type": "application/json"
  },
  "requires_auth": true
}