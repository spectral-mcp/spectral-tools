{
  "name": "cancel_zone_overlay",
  "description": "Cancels/removes a temporary heating override for a specific zone, restoring the zone to its normal schedule. The zone will resume following its scheduled temperature setpoints after the next time block begins.",
  "parameters": {
    "properties": {
      "home_id": {
        "description": "The unique identifier for the home/installation",
        "type": "string"
      },
      "zone_id": {
        "description": "The unique identifier for the heating zone/room to cancel the overlay for",
        "type": "string"
      }
    },
    "required": [
      "home_id",
      "zone_id"
    ],
    "type": "object"
  },
  "request": {
    "method": "DELETE",
    "url": "https://my.tado.com/api/v2/homes/{home_id}/zones/{zone_id}/overlay?ngsw-bypass=true",
    "headers": {
      "accept": "application/json, text/plain, */*"
    },
    "query": {},
    "body": null,
    "content_type": "application/json"
  },
  "requires_auth": true
}